需求说明书：小红书图文内容批量生成。

图片节点具备以下功能：
  具备可视化可操作的工作台
  有模板功能，用户可以自定义图片模板。
  支持上传图片文件
  支持从图片库中选择图片
  支持自定义图片大小和分辨率

节点设计:
不要设计成一个超级节点。
例如：
XHS Export
各节点负责单一职责。

comfyui插件要求：
要求在保证性能的同时，具备良好的架构可扩展理念，低耦合性，原则上每个代码文件不超过300行，不能为了凑 300 行而机械拆文件。
优先去github等开源平台查找相关代码，避免重复造轮子。要求代码质量高，注释详细。
插件使用清爽，操作简单。


项目开发规则：
1. 每个代码文件不得超过300行
2. 不允许为了实现新功能直接修改多个无关模块
3. 不允许跨模块直接访问内部实现
4. UI层不能直接操作文件系统
5. UI层不能直接调用ComfyUI内部实现
6. 所有业务逻辑必须经过Service接口
7. 数据结构必须定义在types/models中
8. 修改已有模块前必须先阅读该模块
9. 修改前必须说明会影响哪些模块
10. 新功能优先新增模块，而不是修改核心模块
11. 禁止为了兼容旧代码加入大量if/else补丁
12. 每完成一个独立功能必须Git commit
13. 不允许一次修改超过一个完整功能范围
14. 不确定架构时先停下来分析，不要直接写代码

你仅有E:\comfyui\ComfyUI\Amake\zxy_xhs文件夹的修改权限，不能在其他文件夹中创建或修改文件。本项目生成在这里，同时要进行单元测试，集成测试。