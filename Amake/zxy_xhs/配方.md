需求说明书：小红书图文内容批量生成。

图片节点具备以下功能：
  具备可视化可操作的工作台
  有模板功能，用户可以自定义图片模板。
  支持上传图片文件
  支持从图片库中选择图片
  支持自定义图片大小和分辨率

节点设计:
不要设计成一个超级节点。
例如：
XHS Export
各节点负责单一职责。

comfyui插件要求：
要求在保证性能的同时，具备良好的架构可扩展理念，低耦合性，原则上每个代码文件不超过300行，不能为了凑 300 行而机械拆文件。
优先去github等开源平台查找相关代码，避免重复造轮子。要求代码质量高，注释详细。
插件使用清爽，操作简单。


项目开发规则：
每个代码文件不得超过300行, 不允许为了实现新功能直接修改多个无关模块,不允许跨模块直接访问内部实现,新功能优先新增模块，
而不是修改核心模块,UI层不能直接调用ComfyUI内部实现,修改已有模块前必须先阅读该模块,不确定架构时先停下来分析，不要直接写代码


测试:
本项目生成在这里，同时要进行单元测试，集成测试。
按照一套强制审计流程 + 静态分析 + 动态测试 + 边界测试 + 回归测试来检查，并且要求“没有证据就不能说通过”。


你仅有E:\comfyui\ComfyUI\Amake\zxy_xhs文件夹的修改权限，不能在其他文件夹中创建或修改文件。